<div class="col-lg-{{$col ?? 6}} {{$groupClass ?? ''}}">
    <div class="form-group {{$groupClass ?? ''}}">
        <button type="submit" class="{{$class ?? ''}}">{{$label ?? __('Submit')}}</button>
    </div>
</div>
